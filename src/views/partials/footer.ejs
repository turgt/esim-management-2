
<% if (typeof user !== 'undefined' && user) { %>
      </div>
      <footer class="py-4 mt-auto text-center border-t border-[var(--border-default)] mx-4 sm:mx-6 lg:mx-8">
        <p class="text-xs text-[var(--text-tertiary)]">&copy; <%= new Date().getFullYear() %> eSIM Hub</p>
      </footer>
    </main>
  </div>
  <div id="toast-container" class="toast-container"></div>
<% } else { %>
  </div>
  </div>
<% } %>

<script>
  if (typeof lucide !== 'undefined') lucide.createIcons();
</script>
</body>
</html>
