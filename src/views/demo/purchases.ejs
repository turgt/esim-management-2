<div class="v2-content">
  <h1 class="v2-page-title v2-anim">eSIM'lerim</h1>

  <!-- Stats -->
  <div class="v2-details v2-anim" style="grid-template-columns:repeat(3,1fr);margin-bottom:24px;">
    <div class="v2-detail" style="border-left:3px solid var(--color-text);">
      <div class="v2-detail-label">Toplam</div>
      <div class="v2-detail-value">6</div>
    </div>
    <div class="v2-detail" style="border-left:3px solid var(--color-success);">
      <div class="v2-detail-label">Aktif</div>
      <div class="v2-detail-value" style="color:var(--color-success);">3</div>
    </div>
    <div class="v2-detail" style="border-left:3px solid var(--color-warning);">
      <div class="v2-detail-label">Bekleyen</div>
      <div class="v2-detail-value" style="color:var(--color-warning);">1</div>
    </div>
  </div>

  <!-- Purchase List -->
  <div class="v2-purchases">
    <% if (locals.purchases && purchases.length) { %>
      <% purchases.forEach(function(purchase) { %>
    <a href="/demo/status" class="v2-row v2-anim">
      <div class="v2-row-flag" style="background:<%= purchase.flagBg %>;">
        <%= purchase.flag %>
      </div>
      <div class="v2-row-info">
        <div class="v2-row-name"><%= purchase.country %></div>
        <div class="v2-row-meta" style="font-family:'SF Mono','Fira Code',monospace;font-size:11px;"><%= purchase.iccid %></div>
      </div>
      <% if (purchase.status === 'active') { %>
        <span class="v2-badge v2-badge-success">Aktif</span>
      <% } else if (purchase.status === 'pending') { %>
        <span class="v2-badge v2-badge-warning">Bekleyen</span>
      <% } else { %>
        <span class="v2-badge v2-badge-danger">Suresi Dolmus</span>
      <% } %>
      <div class="v2-row-arrow">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"/></svg>
      </div>
    </a>
      <% }) %>
    <% } else { %>
    <div style="text-align:center;padding:40px 20px;color:var(--color-text-muted);font-size:14px;">
      Henuz eSIM satin almadiniz.
    </div>
    <% } %>
  </div>
</div>
