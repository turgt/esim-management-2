<!DOCTYPE html>
<html lang="en" class="<%= (typeof user !== 'undefined' && user && user.theme === 'dark') ? 'dark' : '' %>">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - eSIM Hub</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/public/styles.css">
  <script src="https://unpkg.com/lucide@latest/dist/umd/lucide.min.js"></script>
  <script>
    (function() {
      var saved = localStorage.getItem('theme');
      if (saved === 'dark' || (!saved && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
        document.documentElement.classList.add('dark');
      } else {
        document.documentElement.classList.remove('dark');
      }
    })();

    function toggleTheme() {
      var isDark = document.documentElement.classList.toggle('dark');
      localStorage.setItem('theme', isDark ? 'dark' : 'light');
      updateThemeIcon(isDark);
      <% if (typeof user !== 'undefined' && user) { %>
      fetch('/profile', {
        method: 'POST',
        headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
        body: 'theme=' + (isDark ? 'dark' : 'light') + '&_csrf=<%= typeof csrfToken !== "undefined" ? csrfToken : "" %>'
      }).catch(function() {});
      <% } %>
    }

    function updateThemeIcon(isDark) {
      document.querySelectorAll('.theme-sun').forEach(function(el) { el.style.display = isDark ? 'block' : 'none'; });
      document.querySelectorAll('.theme-moon').forEach(function(el) { el.style.display = isDark ? 'none' : 'block'; });
    }

    function copyToClipboard(text, btn) {
      navigator.clipboard.writeText(text).then(function() {
        var icon = btn.querySelector('[data-lucide]');
        if (icon) {
          icon.setAttribute('data-lucide', 'check');
          lucide.createIcons();
          setTimeout(function() {
            icon.setAttribute('data-lucide', 'copy');
            lucide.createIcons();
          }, 1500);
        }
      });
    }

    var _sidebarOpen = false;

    function openSidebar() {
      var overlay = document.getElementById('sidebarOverlay');
      if (!overlay) return;
      _sidebarOpen = true;
      overlay.classList.add('open');
      document.body.style.overflow = 'hidden';
    }

    function closeSidebar() {
      var overlay = document.getElementById('sidebarOverlay');
      if (!overlay) return;
      _sidebarOpen = false;
      overlay.classList.remove('open');
      document.body.style.overflow = '';
    }

    function toggleSidebar(e) {
      if (e) e.stopPropagation();
      if (_sidebarOpen) {
        closeSidebar();
      } else {
        openSidebar();
      }
    }

    function showToast(message, type) {
      type = type || 'success';
      var container = document.getElementById('toast-container');
      if (!container) return;
      var toast = document.createElement('div');
      toast.className = 'toast toast-' + type;
      toast.textContent = message;
      container.appendChild(toast);
      setTimeout(function() {
        toast.style.opacity = '0';
        toast.style.transition = 'opacity 0.3s';
        setTimeout(function() { toast.remove(); }, 300);
      }, 4000);
    }
  </script>
</head>

<% if (typeof user !== 'undefined' && user) { %>
<%/* ===== AUTHENTICATED: Sidebar Layout ===== */%>
<body class="bg-[var(--surface-1)] text-[var(--text-primary)] font-sans antialiased">
  <div class="flex min-h-screen">

    <!-- Desktop Sidebar -->
    <aside class="sidebar hidden md:flex">
      <div class="sidebar-brand">
        <div class="sidebar-brand-icon">
          <i data-lucide="signal" class="w-4.5 h-4.5"></i>
        </div>
        <a href="/offers" class="text-lg font-bold text-[var(--text-primary)] no-underline tracking-tight">eSIM Hub</a>
      </div>

      <nav class="sidebar-nav space-y-0.5">
        <a href="/offers" class="sidebar-link <%= typeof title !== 'undefined' && title === 'Offers' ? 'active' : '' %>">
          <i data-lucide="globe" class="w-[1.125rem] h-[1.125rem]"></i> Plans
        </a>
        <a href="/purchases" class="sidebar-link <%= typeof title !== 'undefined' && (title === 'My eSIMs' || title === 'My Purchases' || title === 'Purchase Status' || title === 'QR Code') ? 'active' : '' %>">
          <i data-lucide="smartphone" class="w-[1.125rem] h-[1.125rem]"></i> My eSIMs
        </a>

        <div class="sidebar-divider"></div>

        <a href="/profile" class="sidebar-link <%= typeof title !== 'undefined' && title === 'Profile' ? 'active' : '' %>">
          <i data-lucide="user" class="w-[1.125rem] h-[1.125rem]"></i> Profile
        </a>

        <% if (user.isAdmin) { %>
        <div class="sidebar-section">Admin</div>
        <a href="/admin/dashboard" class="sidebar-link <%= typeof title !== 'undefined' && title.includes && title.includes('Dashboard') ? 'active' : '' %>">
          <i data-lucide="layout-dashboard" class="w-[1.125rem] h-[1.125rem]"></i> Dashboard
        </a>
        <a href="/admin/users" class="sidebar-link <%= typeof title !== 'undefined' && title.includes && (title.includes('User') || title === 'Manage Users' || title === 'Admin Panel') ? 'active' : '' %>">
          <i data-lucide="users" class="w-[1.125rem] h-[1.125rem]"></i> Users
        </a>
        <a href="/admin/esims" class="sidebar-link <%= typeof title !== 'undefined' && title.includes && (title === 'All eSIMs' || title === 'eSIM Detail' || title === 'Assign eSIM' || title.includes('Top-up') || title.includes('Top Up')) ? 'active' : '' %>">
          <i data-lucide="layers" class="w-[1.125rem] h-[1.125rem]"></i> All eSIMs
        </a>
        <% } %>
      </nav>

      <div class="sidebar-footer">
        <div class="sidebar-actions">
          <button onclick="toggleTheme()" class="p-1.5 rounded-lg text-[var(--text-tertiary)] hover:bg-[var(--surface-2)] hover:text-[var(--text-primary)]" title="Toggle theme">
            <i data-lucide="sun" class="w-4 h-4 theme-sun" style="display:none;"></i>
            <i data-lucide="moon" class="w-4 h-4 theme-moon"></i>
          </button>
          <a href="/auth/logout" class="p-1.5 rounded-lg text-[var(--text-tertiary)] hover:bg-[var(--surface-2)] hover:text-[var(--danger)]" title="Logout">
            <i data-lucide="log-out" class="w-4 h-4"></i>
          </a>
        </div>
        <div class="sidebar-user">
          <div class="avatar avatar-sm" style="background: linear-gradient(135deg, #4f46e5, #7c3aed);">
            <%= (user.displayName || user.username).charAt(0).toUpperCase() %>
          </div>
          <div class="min-w-0 flex-1">
            <p class="text-sm font-medium text-[var(--text-primary)] truncate"><%= user.displayName || user.username %></p>
            <% if (user.isAdmin) { %>
            <p class="text-[0.6875rem] text-[var(--brand-primary)] font-medium">Admin</p>
            <% } else { %>
            <p class="text-[0.6875rem] text-[var(--text-tertiary)]">@<%= user.username %></p>
            <% } %>
          </div>
        </div>
      </div>
    </aside>

    <!-- Mobile Sidebar Overlay -->
    <div id="sidebarOverlay" class="sidebar-overlay" onclick="if(event.target===this)closeSidebar()">
      <aside class="sidebar-mobile">
        <div class="sidebar-brand">
          <div class="sidebar-brand-icon">
            <i data-lucide="signal" class="w-4.5 h-4.5"></i>
          </div>
          <span class="text-lg font-bold text-[var(--text-primary)] tracking-tight flex-1">eSIM Hub</span>
          <button onclick="closeSidebar()" class="p-1.5 rounded-lg text-[var(--text-tertiary)] hover:bg-[var(--surface-2)]">
            <i data-lucide="x" class="w-5 h-5"></i>
          </button>
        </div>

        <nav class="sidebar-nav space-y-0.5 flex-1">
          <a href="/offers" class="sidebar-link <%= typeof title !== 'undefined' && title === 'Offers' ? 'active' : '' %>">
            <i data-lucide="globe" class="w-[1.125rem] h-[1.125rem]"></i> Plans
          </a>
          <a href="/purchases" class="sidebar-link <%= typeof title !== 'undefined' && (title === 'My eSIMs' || title === 'My Purchases' || title === 'Purchase Status' || title === 'QR Code') ? 'active' : '' %>">
            <i data-lucide="smartphone" class="w-[1.125rem] h-[1.125rem]"></i> My eSIMs
          </a>
          <div class="sidebar-divider"></div>
          <a href="/profile" class="sidebar-link <%= typeof title !== 'undefined' && title === 'Profile' ? 'active' : '' %>">
            <i data-lucide="user" class="w-[1.125rem] h-[1.125rem]"></i> Profile
          </a>

          <% if (user.isAdmin) { %>
          <div class="sidebar-section">Admin</div>
          <a href="/admin/dashboard" class="sidebar-link <%= typeof title !== 'undefined' && title.includes && title.includes('Dashboard') ? 'active' : '' %>">
            <i data-lucide="layout-dashboard" class="w-[1.125rem] h-[1.125rem]"></i> Dashboard
          </a>
          <a href="/admin/users" class="sidebar-link <%= typeof title !== 'undefined' && title.includes && (title.includes('User') || title === 'Manage Users' || title === 'Admin Panel') ? 'active' : '' %>">
            <i data-lucide="users" class="w-[1.125rem] h-[1.125rem]"></i> Users
          </a>
          <a href="/admin/esims" class="sidebar-link <%= typeof title !== 'undefined' && title.includes && (title === 'All eSIMs' || title === 'eSIM Detail' || title === 'Assign eSIM' || title.includes('Top-up') || title.includes('Top Up')) ? 'active' : '' %>">
            <i data-lucide="layers" class="w-[1.125rem] h-[1.125rem]"></i> All eSIMs
          </a>
          <% } %>
        </nav>

        <div class="sidebar-footer">
          <div class="sidebar-actions">
            <button onclick="toggleTheme()" class="p-1.5 rounded-lg text-[var(--text-tertiary)] hover:bg-[var(--surface-2)]">
              <i data-lucide="sun" class="w-4 h-4 theme-sun" style="display:none;"></i>
              <i data-lucide="moon" class="w-4 h-4 theme-moon"></i>
            </button>
            <a href="/auth/logout" class="p-1.5 rounded-lg text-[var(--text-tertiary)] hover:bg-[var(--surface-2)] hover:text-[var(--danger)]">
              <i data-lucide="log-out" class="w-4 h-4"></i>
            </a>
          </div>
          <div class="sidebar-user">
            <div class="avatar avatar-sm" style="background: linear-gradient(135deg, #4f46e5, #7c3aed);">
              <%= (user.displayName || user.username).charAt(0).toUpperCase() %>
            </div>
            <span class="text-sm font-medium text-[var(--text-primary)] truncate"><%= user.displayName || user.username %></span>
          </div>
        </div>
      </aside>
    </div>

    <!-- Main Content Area -->
    <main class="flex-1 min-w-0 flex flex-col min-h-screen">
      <!-- Mobile Header -->
      <div class="mobile-header md:hidden">
        <div class="flex items-center gap-2.5">
          <div class="sidebar-brand-icon !w-7 !h-7 !rounded-lg" style="box-shadow:none;">
            <i data-lucide="signal" class="w-3.5 h-3.5"></i>
          </div>
          <span class="text-base font-bold text-[var(--text-primary)] tracking-tight">eSIM Hub</span>
        </div>
        <button type="button" onclick="toggleSidebar(event)" class="p-2 -mr-1 rounded-lg text-[var(--text-secondary)] hover:bg-[var(--surface-2)] hover:text-[var(--text-primary)]">
          <i data-lucide="menu" class="w-5 h-5"></i>
        </button>
      </div>

      <% if (typeof user !== 'undefined' && user && user.emailVerified === false && user.email) { %>
      <div class="bg-amber-50 dark:bg-amber-500/10 border-b border-amber-200 dark:border-amber-500/20 text-amber-800 dark:text-amber-300 text-center py-2.5 text-sm font-medium">
        <i data-lucide="mail-warning" class="w-4 h-4 inline-block -mt-0.5 mr-1"></i>
        Your email is not verified. Check your inbox for the verification link.
      </div>
      <% } %>

      <div class="flex-1 w-full max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-8">

<% } else { %>
<%/* ===== AUTH: Centered Layout ===== */%>
<body class="bg-[var(--surface-1)] text-[var(--text-primary)] font-sans antialiased">
  <div class="auth-wrapper">
    <div class="auth-card">
      <div class="auth-brand">
        <div class="auth-brand-icon">
          <i data-lucide="signal" class="w-6 h-6"></i>
        </div>
        <span class="text-xl font-bold text-[var(--text-primary)] tracking-tight">eSIM Hub</span>
      </div>

<% } %>

<script>
document.addEventListener('DOMContentLoaded', function() {
  lucide.createIcons();
  var isDark = document.documentElement.classList.contains('dark');
  updateThemeIcon(isDark);
});
</script>
