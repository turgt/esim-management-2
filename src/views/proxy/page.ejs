<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <title><%= title %></title>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --brand: #4f46e5;
      --brand-light: #eef2ff;
      --brand-dark: #3730a3;
      --surface: #f8fafc;
      --card: #ffffff;
      --text: #0f172a;
      --text-muted: #64748b;
      --border: #e2e8f0;
      --success: #16a34a;
      --success-light: #dcfce7;
      --warning: #d97706;
      --warning-light: #fef3c7;
      --danger: #dc2626;
      --danger-light: #fee2e2;
      --radius: 16px;
      --shadow: 0 4px 24px rgba(0,0,0,0.08);
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: var(--surface);
      color: var(--text);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    /* ===== HEADER / BRANDING ===== */
    .page-header {
      background: var(--card);
      border-bottom: 1px solid var(--border);
      padding: 16px 20px;
      display: flex;
      align-items: center;
      gap: 12px;
    }
    .agency-logo {
      width: 40px;
      height: 40px;
      border-radius: 10px;
      object-fit: contain;
      background: var(--brand-light);
    }
    .agency-logo-placeholder {
      width: 40px;
      height: 40px;
      border-radius: 10px;
      background: var(--brand);
      color: #fff;
      font-size: 18px;
      font-weight: 700;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
    }
    .agency-name {
      font-size: 16px;
      font-weight: 700;
      color: var(--text);
    }
    .agency-powered {
      font-size: 11px;
      color: var(--text-muted);
    }

    /* ===== MAIN CONTENT ===== */
    .main {
      flex: 1;
      padding: 24px 20px;
      max-width: 480px;
      width: 100%;
      margin: 0 auto;
    }

    /* ===== CARD ===== */
    .card {
      background: var(--card);
      border-radius: var(--radius);
      box-shadow: var(--shadow);
      padding: 24px;
      margin-bottom: 16px;
    }

    /* ===== STATUS BADGE ===== */
    .status-badge {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 4px 12px;
      border-radius: 99px;
      font-size: 12px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      margin-bottom: 16px;
    }
    .status-badge.preparing { background: var(--warning-light); color: var(--warning); }
    .status-badge.ready     { background: var(--success-light); color: var(--success); }
    .status-badge.done      { background: #dbeafe; color: #1d4ed8; }
    .status-badge.error     { background: var(--danger-light); color: var(--danger); }

    .status-dot {
      width: 7px; height: 7px;
      border-radius: 50%;
      background: currentColor;
      animation: pulse-dot 1.5s ease-in-out infinite;
    }
    @keyframes pulse-dot {
      0%, 100% { opacity: 1; }
      50% { opacity: 0.3; }
    }

    /* ===== HEADINGS ===== */
    .card-title {
      font-size: 20px;
      font-weight: 700;
      margin-bottom: 8px;
      line-height: 1.3;
    }
    .card-subtitle {
      font-size: 14px;
      color: var(--text-muted);
      line-height: 1.5;
    }

    /* ===== COUNTDOWN ===== */
    .countdown-wrap {
      display: flex;
      gap: 12px;
      justify-content: center;
      margin: 24px 0 8px;
    }
    .countdown-unit {
      display: flex;
      flex-direction: column;
      align-items: center;
      background: var(--brand-light);
      border-radius: 12px;
      padding: 14px 18px;
      min-width: 64px;
    }
    .countdown-number {
      font-size: 28px;
      font-weight: 800;
      color: var(--brand);
      line-height: 1;
      font-variant-numeric: tabular-nums;
    }
    .countdown-label {
      font-size: 10px;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 0.05em;
      margin-top: 4px;
    }
    .countdown-note {
      text-align: center;
      font-size: 13px;
      color: var(--text-muted);
      margin-bottom: 8px;
    }

    /* ===== INSTALL BUTTON ===== */
    .btn-primary {
      display: block;
      width: 100%;
      background: var(--brand);
      color: #fff;
      border: none;
      border-radius: 12px;
      padding: 16px;
      font-size: 16px;
      font-weight: 700;
      text-align: center;
      text-decoration: none;
      cursor: pointer;
      transition: background 0.15s;
      margin-top: 20px;
    }
    .btn-primary:active { background: var(--brand-dark); }

    /* ===== QR CODE ===== */
    .qr-wrap {
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 20px;
      background: var(--surface);
      border-radius: 12px;
      margin: 20px 0;
    }
    .qr-wrap img {
      width: 200px;
      height: 200px;
      border-radius: 8px;
      border: 1px solid var(--border);
    }
    .qr-label {
      font-size: 12px;
      color: var(--text-muted);
      margin-top: 8px;
    }

    /* ===== DETAIL ROWS ===== */
    .detail-list {
      list-style: none;
      margin-top: 16px;
    }
    .detail-list li {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      gap: 12px;
      padding: 10px 0;
      border-bottom: 1px solid var(--border);
      font-size: 13px;
    }
    .detail-list li:last-child { border-bottom: none; }
    .detail-label { color: var(--text-muted); flex-shrink: 0; }
    .detail-value {
      font-weight: 600;
      text-align: right;
      word-break: break-all;
      max-width: 260px;
    }
    .copy-btn {
      background: none;
      border: 1px solid var(--border);
      border-radius: 6px;
      padding: 2px 8px;
      font-size: 11px;
      color: var(--brand);
      cursor: pointer;
      flex-shrink: 0;
      transition: background 0.1s;
    }
    .copy-btn:active { background: var(--brand-light); }

    /* ===== ACCORDION ===== */
    details {
      border: 1px solid var(--border);
      border-radius: 12px;
      overflow: hidden;
      margin-top: 12px;
    }
    summary {
      padding: 14px 16px;
      cursor: pointer;
      font-size: 14px;
      font-weight: 600;
      list-style: none;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    summary::after { content: '›'; font-size: 18px; color: var(--text-muted); }
    details[open] summary::after { content: '∨'; }
    .accordion-body {
      padding: 0 16px 16px;
    }
    .accordion-body p, .accordion-body li {
      font-size: 13px;
      color: var(--text-muted);
      line-height: 1.6;
    }
    .accordion-body ol, .accordion-body ul { padding-left: 18px; margin-top: 8px; }
    .accordion-body li { margin-bottom: 6px; }

    /* ===== CONTACT CARD ===== */
    .contact-card {
      display: flex;
      flex-direction: column;
      gap: 8px;
      margin-top: 8px;
    }
    .contact-item {
      display: flex;
      align-items: center;
      gap: 10px;
      font-size: 14px;
    }
    .contact-icon {
      width: 32px; height: 32px;
      border-radius: 8px;
      background: var(--brand-light);
      color: var(--brand);
      font-size: 16px;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
    }
    .contact-item a { color: var(--brand); text-decoration: none; }

    /* ===== SUCCESS / INSTALLED ===== */
    .success-icon {
      width: 64px; height: 64px;
      border-radius: 50%;
      background: var(--success-light);
      color: var(--success);
      font-size: 30px;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 16px;
    }

    /* ===== ERROR STATE ===== */
    .error-icon {
      width: 64px; height: 64px;
      border-radius: 50%;
      background: var(--danger-light);
      color: var(--danger);
      font-size: 30px;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 16px;
    }

    /* ===== FOOTER ===== */
    .page-footer {
      text-align: center;
      padding: 16px 20px 32px;
      font-size: 12px;
      color: var(--text-muted);
    }

    /* ===== POLLING INDICATOR ===== */
    .poll-indicator {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      margin-top: 20px;
      font-size: 12px;
      color: var(--text-muted);
    }
    .poll-spinner {
      width: 14px; height: 14px;
      border: 2px solid var(--border);
      border-top-color: var(--brand);
      border-radius: 50%;
      animation: spin 0.8s linear infinite;
    }
    @keyframes spin { to { transform: rotate(360deg); } }

    @media (min-width: 480px) {
      .main { padding: 32px 20px; }
    }
  </style>
</head>
<body>

  <!-- ===== HEADER / BRANDING ===== -->
  <header class="page-header">
    <% if (agency && agency.logoUrl) { %>
    <img class="agency-logo" src="<%= agency.logoUrl %>" alt="<%= agency.name %>" onerror="this.style.display='none'">
    <% } else if (agency) { %>
    <div class="agency-logo-placeholder"><%= agency.name.charAt(0).toUpperCase() %></div>
    <% } else { %>
    <div class="agency-logo-placeholder">✦</div>
    <% } %>
    <div>
      <div class="agency-name"><%= agency ? agency.name : 'DataPatch' %></div>
      <div class="agency-powered">eSIM Service</div>
    </div>
  </header>

  <main class="main">

    <% if (state === 'not_found') { %>
    <!-- ===== NOT FOUND ===== -->
    <div class="card" style="text-align:center;">
      <div class="error-icon">✕</div>
      <div class="card-title">Invalid Link</div>
      <p class="card-subtitle" style="margin-top:8px;">
        This link is invalid or has expired. Please contact your agency.
      </p>
    </div>

    <% } else if (state === 'pending_provisioning') { %>
    <!-- ===== PENDING PROVISIONING ===== -->
    <div class="card">
      <div class="status-badge preparing"><span class="status-dot"></span>Preparing</div>
      <div class="card-title">Your eSIM is being prepared</div>
      <p class="card-subtitle">
        Your eSIM is being prepared in our system. Please wait, the installation link will be updated automatically.
      </p>

      <% if (booking && booking.dueDate) { %>
      <div class="countdown-wrap" id="countdown-wrap">
        <div class="countdown-unit"><span class="countdown-number" id="cd-h">--</span><span class="countdown-label">Hours</span></div>
        <div class="countdown-unit"><span class="countdown-number" id="cd-m">--</span><span class="countdown-label">Min</span></div>
        <div class="countdown-unit"><span class="countdown-number" id="cd-s">--</span><span class="countdown-label">Sec</span></div>
      </div>
      <p class="countdown-note" id="countdown-note">Estimated time until ready</p>
      <% } %>

      <div class="poll-indicator" id="poll-indicator">
        <div class="poll-spinner"></div>
        <span>Checking status...</span>
      </div>
    </div>

    <% if (booking && booking.travelerName) { %>
    <div class="card">
      <p style="font-size:14px;color:var(--text-muted);">
        Hello <strong><%= booking.travelerName %></strong>, this page will automatically update when your eSIM is ready.
      </p>
    </div>
    <% } %>

    <script>
    (function() {
      <% if (booking && booking.dueDate) { %>
      var dueDate = new Date('<%= new Date(booking.dueDate).toISOString() %>');
      function tick() {
        var now = new Date();
        var diff = Math.max(0, dueDate - now);
        var h = Math.floor(diff / 3600000);
        var m = Math.floor((diff % 3600000) / 60000);
        var s = Math.floor((diff % 60000) / 1000);
        document.getElementById('cd-h').textContent = String(h).padStart(2,'0');
        document.getElementById('cd-m').textContent = String(m).padStart(2,'0');
        document.getElementById('cd-s').textContent = String(s).padStart(2,'0');
        if (diff === 0) {
          document.getElementById('countdown-note').textContent = 'Expected time has passed, not ready yet.';
        }
      }
      tick();
      setInterval(tick, 1000);
      <% } %>

      // Poll status every 30 seconds
      function pollStatus() {
        fetch('/api/booking-status/<%= booking ? booking.token : "" %>')
          .then(function(r) { return r.json(); })
          .then(function(data) {
            if (data.status && data.status !== 'pending_provisioning') {
              window.location.reload();
            }
          })
          .catch(function() {});
      }
      setInterval(pollStatus, 30000);
    })();
    </script>

    <% } else if (state === 'provisioned') { %>
    <!-- ===== PROVISIONED: INSTALL ===== -->
    <div class="card">
      <div class="status-badge ready">✓ Ready</div>
      <div class="card-title">Your eSIM is ready to install!</div>
      <p class="card-subtitle">
        Tap the button below to add the eSIM to your phone, or scan the QR code.
      </p>

      <% if (esim && esim.smdpAddress && esim.activationCode) { %>
      <%
        var lpa = 'LPA:1$' + esim.smdpAddress + '$' + esim.activationCode;
      %>
      <a href="<%= lpa %>" class="btn-primary">📲 Install eSIM</a>

      <div class="qr-wrap">
        <%
          var qrToken = (booking && booking.token) ? booking.token : '';
        %>
        <img
          src="/e/<%= qrToken %>/qr"
          alt="QR Code"
          onerror="this.parentElement.style.display='none'"
        >
        <span class="qr-label">Scan the QR code with your camera app</span>
      </div>

      <ul class="detail-list">
        <li>
          <span class="detail-label">ICCID</span>
          <span class="detail-value" id="val-iccid"><%= esim.iccid || '—' %></span>
          <button class="copy-btn" onclick="copyText('<%= esim.iccid %>', this)">Copy</button>
        </li>
        <li>
          <span class="detail-label">SM-DP+ Address</span>
          <span class="detail-value" id="val-smdp"><%= esim.smdpAddress || '—' %></span>
          <button class="copy-btn" onclick="copyText('<%= esim.smdpAddress %>', this)">Copy</button>
        </li>
        <li>
          <span class="detail-label">Activation Code</span>
          <span class="detail-value" id="val-ac"><%= esim.activationCode || '—' %></span>
          <button class="copy-btn" onclick="copyText('<%= esim.activationCode %>', this)">Copy</button>
        </li>
      </ul>
      <% } else { %>
      <p class="card-subtitle" style="margin-top:16px;color:var(--danger);">eSIM details are not ready yet. Please try again shortly.</p>
      <% } %>
    </div>

    <% if (package) { %>
    <div class="card">
      <p style="font-size:13px;font-weight:600;color:var(--text-muted);margin-bottom:12px;">PACKAGE DETAILS</p>
      <ul class="detail-list">
        <li><span class="detail-label">Package</span><span class="detail-value"><%= package.title || '—' %></span></li>
        <li><span class="detail-label">Data</span><span class="detail-value"><%= package.isUnlimited ? 'Unlimited' : (package.data ? package.data + ' MB' : '—') %></span></li>
        <li><span class="detail-label">Duration</span><span class="detail-value"><%= package.day ? package.day + ' Days' : '—' %></span></li>
        <li><span class="detail-label">Operator</span><span class="detail-value"><%= package.operatorTitle || '—' %></span></li>
        <% if (package.voice) { %><li><span class="detail-label">Voice</span><span class="detail-value"><%= package.voice %></span></li><% } %>
        <% if (package.text) { %><li><span class="detail-label">SMS</span><span class="detail-value"><%= package.text %></span></li><% } %>
      </ul>
    </div>
    <% } %>

    <details>
      <summary>Manual installation steps</summary>
      <div class="accordion-body">
        <p><strong>iPhone (iOS 12.1+):</strong></p>
        <ol>
          <li>Go to Settings → Cellular → Add eSIM</li>
          <li>Tap "Use QR Code"</li>
          <li>Scan the QR code above with your camera</li>
          <li>Confirm the activation</li>
        </ol>
        <p style="margin-top:12px;"><strong>Android:</strong></p>
        <ol>
          <li>Go to Settings → Connections → SIM Card Manager</li>
          <li>Select Add eSIM → Add using QR Code</li>
          <li>Scan the QR code above</li>
          <li>Follow the activation instructions</li>
        </ol>
      </div>
    </details>

    <% } else if (state === 'installed') { %>
    <!-- ===== INSTALLED / CONFIRMED ===== -->
    <div class="card" style="text-align:center;">
      <div class="success-icon">✓</div>
      <div class="card-title">eSIM Installed!</div>
      <p class="card-subtitle" style="margin-top:8px;">
        Your eSIM has been successfully installed. Enjoy your travels with great connectivity.
      </p>
    </div>

    <details>
      <summary>Having issues?</summary>
      <div class="accordion-body">
        <p><strong>If you're experiencing connection issues:</strong></p>
        <ul>
          <li>Toggle airplane mode on and off</li>
          <li>Check that data roaming is enabled in Settings</li>
          <li>Make sure the eSIM is selected as the active SIM</li>
          <li>Restart your phone</li>
        </ul>
        <p style="margin-top:12px;">If the issue persists, please contact your agency.</p>
      </div>
    </details>

    <% if (agency && (agency.contactEmail || agency.phone)) { %>
    <div class="card">
      <p style="font-size:13px;font-weight:600;color:var(--text-muted);margin-bottom:12px;">SUPPORT</p>
      <div class="contact-card">
        <% if (agency.contactName) { %>
        <div class="contact-item">
          <div class="contact-icon">👤</div>
          <span><%= agency.contactName %></span>
        </div>
        <% } %>
        <% if (agency.contactEmail) { %>
        <div class="contact-item">
          <div class="contact-icon">✉</div>
          <a href="mailto:<%= agency.contactEmail %>"><%= agency.contactEmail %></a>
        </div>
        <% } %>
        <% if (agency.phone) { %>
        <div class="contact-item">
          <div class="contact-icon">📞</div>
          <a href="tel:<%= agency.phone %>"><%= agency.phone %></a>
        </div>
        <% } %>
      </div>
    </div>
    <% } %>

    <% } else if (state === 'cancelled' || state === 'failed' || state === 'expired' || state === 'error') { %>
    <!-- ===== ERROR / CANCELLED / EXPIRED ===== -->
    <div class="card" style="text-align:center;">
      <div class="error-icon">!</div>
      <div class="card-title">
        <% if (state === 'cancelled') { %>eSIM Cancelled
        <% } else if (state === 'expired') { %>eSIM Expired
        <% } else { %>Something Went Wrong<% } %>
      </div>
      <p class="card-subtitle" style="margin-top:8px;">
        <% if (state === 'cancelled') { %>
          This eSIM booking has been cancelled. Please contact your agency for more information.
        <% } else if (state === 'expired') { %>
          The 30-day installation period for this eSIM has expired. Please contact your agency for a new eSIM.
        <% } else { %>
          An error occurred with your eSIM. Please contact your agency.
        <% } %>
      </p>
    </div>

    <% if (agency && (agency.contactEmail || agency.phone || agency.contactName)) { %>
    <div class="card">
      <p style="font-size:13px;font-weight:600;color:var(--text-muted);margin-bottom:12px;">CONTACT</p>
      <div class="contact-card">
        <% if (agency.contactName) { %>
        <div class="contact-item">
          <div class="contact-icon">👤</div>
          <span><%= agency.contactName %></span>
        </div>
        <% } %>
        <% if (agency.contactEmail) { %>
        <div class="contact-item">
          <div class="contact-icon">✉</div>
          <a href="mailto:<%= agency.contactEmail %>"><%= agency.contactEmail %></a>
        </div>
        <% } %>
        <% if (agency.phone) { %>
        <div class="contact-item">
          <div class="contact-icon">📞</div>
          <a href="tel:<%= agency.phone %>"><%= agency.phone %></a>
        </div>
        <% } %>
      </div>
    </div>
    <% } %>

    <% } %>

  </main>

  <footer class="page-footer">
    Powered by DataPatch eSIM Service
  </footer>

  <script>
  function copyText(text, btn) {
    if (!text) return;
    navigator.clipboard.writeText(text).then(function() {
      var orig = btn.textContent;
      btn.textContent = 'Copied!';
      btn.style.color = 'var(--success)';
      setTimeout(function() {
        btn.textContent = orig;
        btn.style.color = '';
      }, 1500);
    }).catch(function() {
      // Fallback for older browsers
      var ta = document.createElement('textarea');
      ta.value = text;
      ta.style.position = 'fixed';
      ta.style.opacity = '0';
      document.body.appendChild(ta);
      ta.select();
      document.execCommand('copy');
      document.body.removeChild(ta);
      btn.textContent = 'Copied!';
      btn.style.color = 'var(--success)';
      setTimeout(function() { btn.textContent = 'Copy'; btn.style.color = ''; }, 1500);
    });
  }
  </script>

</body>
</html>
